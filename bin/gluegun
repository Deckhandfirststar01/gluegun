#!/usr/bin/env node

// hook into ts-node so we can run typescript on the fly
require('ts-node').register({ project: `${__dirname}/../tsconfig.json` })

// kick off gluegun
require(`${__dirname}/../src/cli/cli`)(process.argv)
